package types

// There are few of these keys, so we don't concern ourselves with small key names.
var (
	LastBlockTimestampKey = []byte("last_block_timestamp/")
	LastDowntimeOfLength  = []byte("last_downtime_of_length/")
)
