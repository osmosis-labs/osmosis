name: Tests builder

on:
  pull_request:
    branches:
      - "**"
  push:
    branches:
      - "main"
      - "concentrated-liquidity-main"
      - "v[0-9]**"
  workflow_dispatch:

jobs:
  e2e-builder:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      -
        name: Build e2e image
        run: make docker-build-debug
